(function(){
  function clean(v){return String(v||'').trim()}
  function esc(v){return String(v||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}
  function slug(v){return clean(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||'section'}
  function langLabel(v){const l=clean(v).toLowerCase().replace(/^language-/,'')||'text';return {sh:'shell',shell:'shell',bash:'bash',zsh:'shell',ps1:'powershell',pwsh:'powershell'}[l]||l}
  function isUrl(v){return /^(https?:|data:|blob:|mailto:|tel:|#)/i.test(clean(v))}
  function assetPath(path){let p=clean(path).split('|')[0].trim();if(!p)return'';if(isUrl(p))return p;if(p.startsWith('/portofiolo/'))return p;if(p.startsWith('/content/')||p.startsWith('content/')||p.startsWith('/assets/')||p.startsWith('assets/'))return p.replace(/^\//,'');return 'content/writeups/media/'+p.replace(/^.*[\\/]/,'')}
  function parseEmbedAttrs(raw){const attrs={};clean(raw).split('|').slice(1).forEach(part=>{part=part.trim();if(/^\d+$/.test(part))attrs.width=part;else if(part.includes('=')){const[k,...rest]=part.split('=');attrs[k.trim()]=rest.join('=').trim()}else if(part)attrs.caption=part});return attrs}
  function mediaHtml(label,raw){const attrs=parseEmbedAttrs(raw),src=assetPath(raw),caption=esc(attrs.caption||label||clean(raw).split('|')[0].split('/').pop()||'Média'),style=attrs.width?' style="max-width:'+esc(attrs.width)+'px"':'';const lower=src.split('?')[0].split('#')[0].toLowerCase();if(/\.(png|jpe?g|gif|webp|svg)$/i.test(lower))return '<figure class="obs-media"'+style+'><img src="'+esc(src)+'" alt="'+caption+'" loading="lazy"><figcaption>'+caption+'</figcaption></figure>';if(/\.(mp4|webm|mov|mkv)$/i.test(lower))return '<figure class="obs-media"><video controls src="'+esc(src)+'"></video><figcaption>'+caption+'</figcaption></figure>';if(/\.(mp3|wav|ogg|m4a|aac|flac)$/i.test(lower))return '<figure class="obs-media"><audio controls src="'+esc(src)+'"></audio><figcaption>'+caption+'</figcaption></figure>';if(/\.pdf$/i.test(lower))return '<p><a class="obs-attachment" href="'+esc(src)+'" target="_blank" rel="noopener">📄 '+caption+'</a></p>';return '<p><a class="obs-attachment" href="'+esc(src)+'" target="_blank" rel="noopener">📎 '+caption+'</a></p>'}
  function transformCallouts(text){const lines=String(text||'').split('\n'),out=[];let i=0;while(i<lines.length){const m=lines[i].match(/^>\s*\[!([A-Za-z0-9_-]+)\]([+-])?\s*(.*)$/);if(!m){out.push(lines[i]);i++;continue}const type=m[1].toLowerCase(),title=m[3]||type,body=[];i++;while(i<lines.length&&/^>/.test(lines[i])){body.push(lines[i].replace(/^>\s?/,''));i++}out.push('<aside class="obs-callout obs-callout-'+esc(type)+'"><div class="obs-callout-title"><span>'+esc(type)+'</span>'+esc(title)+'</div><div class="obs-callout-body">');out.push(body.join('\n'));out.push('</div></aside>')}return out.join('\n')}
  function preprocess(md){let text=String(md||'').replace(/\r\n/g,'\n');text=text.replace(/%%[\s\S]*?%%/g,'');text=text.replace(/!\[\[([^\]]+)\]\]/g,function(_,ref){return '\n'+mediaHtml(ref,ref)+'\n'});text=text.replace(/!\[([^\]]*)\]\(([^)]+)\)/g,function(_,alt,url){return '\n'+mediaHtml(alt,url)+'\n'});text=text.replace(/==([^\n]+?)==/g,'<mark>$1</mark>');text=text.replace(/\[\[([^\]|]+)\|([^\]]+)\]\]/g,'<span class="obs-wikilink" data-target="$1">$2</span>');text=text.replace(/\[\[([^\]]+)\]\]/g,'<span class="obs-wikilink">$1</span>');return transformCallouts(text)}
  function codeBlock(code,language){const l=langLabel(language);const label=l==='text'?'Code':l.charAt(0).toUpperCase()+l.slice(1);return '<pre class="obs-code" data-lang="'+esc(label)+'"><button class="obs-copy-code" type="button" aria-label="Copier le code">Copier</button><span class="obs-code-lang">'+esc(label)+'</span><code class="language-'+esc(l)+'">'+esc(code)+'</code></pre>'}
  function mermaidBlock(code){return '<div class="obs-mermaid-wrap"><div class="obs-mermaid mermaid">'+esc(code)+'</div></div>'}
  function buildEngine(){if(!window.markdownit)return null;const md=window.markdownit({html:true,linkify:true,typographer:true,breaks:false});const defaultHeadingOpen=md.renderer.rules.heading_open||function(tokens,idx,options,env,self){return self.renderToken(tokens,idx,options)};md.renderer.rules.heading_open=function(tokens,idx,options,env,self){const next=tokens[idx+1];if(next&&next.type==='inline')tokens[idx].attrSet('id',slug(next.content));tokens[idx].attrJoin('class','obs-heading obs-'+tokens[idx].tag);return defaultHeadingOpen(tokens,idx,options,env,self)};md.renderer.rules.fence=function(tokens,idx){const token=tokens[idx],info=clean(token.info).split(/\s+/)[0],l=info.toLowerCase(),code=token.content||'';if(['mermaid','flowchart','graph','sequence'].includes(l)||/^\s*(flowchart|graph|sequenceDiagram|classDiagram|stateDiagram|erDiagram|journey|gantt|pie|mindmap|timeline)\b/.test(code))return mermaidBlock(code);return codeBlock(code,info)};md.renderer.rules.code_block=function(tokens,idx){return codeBlock(tokens[idx].content,'text')};return md}
  function postprocess(html){return String(html||'').replace(/<li>\s*\[ \]\s*/g,'<li class="obs-task"><input type="checkbox" disabled> <span>').replace(/<li>\s*\[[xX]\]\s*/g,'<li class="obs-task"><input type="checkbox" disabled checked> <span>').replace(/(<li class="obs-task"><input[^>]+> <span>)([\s\S]*?)<\/li>/g,'$1$2</span></li>')}
  function fallback(md){return preprocess(md).split(/\n\s*\n/).map(p=>'<p>'+p+'</p>').join('\n')}
  function render(md){const text=preprocess(md),engine=buildEngine();return postprocess(engine?engine.render(text):fallback(text))}
  function loadScriptOnce(key,src){if(window[key])return Promise.resolve(window[key]);const loading='__'+key+'Loading';if(window[loading])return window[loading];window[loading]=new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=src;s.onload=()=>resolve(window[key]);s.onerror=reject;document.head.appendChild(s)});return window[loading]}
  function runHighlight(root){const nodes=Array.from((root||document).querySelectorAll('pre.obs-code code:not([data-highlighted="yes"])'));if(!nodes.length)return;loadScriptOnce('hljs','https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11.9.0/build/highlight.min.js').then(h=>{nodes.forEach(code=>{try{h.highlightElement(code)}catch(e){}code.dataset.highlighted='yes'})}).catch(()=>nodes.forEach(code=>code.dataset.highlighted='yes'))}
  function runMermaid(root){const nodes=Array.from((root||document).querySelectorAll('.obs-mermaid:not([data-processed="true"])'));if(!nodes.length)return;loadScriptOnce('mermaid','https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js').then(m=>{try{m.initialize({startOnLoad:false,theme:'dark',securityLevel:'loose',flowchart:{htmlLabels:true,curve:'basis'}});m.run({nodes})}catch(e){nodes.forEach(n=>n.classList.add('obs-mermaid-failed'))}}).catch(()=>nodes.forEach(n=>n.classList.add('obs-mermaid-failed')))}
  function addCopyButtons(root){Array.from((root||document).querySelectorAll('pre.obs-code')).forEach(pre=>{const btn=pre.querySelector('.obs-copy-code');const code=pre.querySelector('code');if(!btn||!code||btn.dataset.ready==='true')return;btn.dataset.ready='true';btn.addEventListener('click',async()=>{const text=code.textContent||'';try{await navigator.clipboard.writeText(text);btn.textContent='Copié';setTimeout(()=>btn.textContent='Copier',1400)}catch(e){const ta=document.createElement('textarea');ta.value=text;document.body.appendChild(ta);ta.select();document.execCommand('copy');ta.remove();btn.textContent='Copié';setTimeout(()=>btn.textContent='Copier',1400)}})})}
  function fitHeadings(root){Array.from((root||document).querySelectorAll('.obsidian-doc h1,.obsidian-doc h2,.obsidian-doc h3,.obsidian-doc h4,.obsidian-doc h5,.obsidian-doc h6')).forEach(h=>{const hasBadge=h.querySelector('span[style],mark,font');if(!hasBadge)return;h.classList.add('obs-heading-nowrap');h.style.removeProperty('--obs-fit-scale');let guard=0;let size=parseFloat(getComputedStyle(h).fontSize)||48;const min=Math.max(22,size*.58);while(h.scrollWidth>h.clientWidth&&size>min&&guard<24){size-=2;h.style.fontSize=size+'px';guard++}if(h.scrollWidth>h.clientWidth)h.classList.add('obs-heading-scroll')})}
  function enhance(root){addCopyButtons(root);fitHeadings(root);runHighlight(root);runMermaid(root)}
  window.ObsidianFidelity={render,enhance,mediaHtml,assetPath};
})();
