(function(){
  const state={mode:'project',writeupSection:'',writeupPlatform:'',writeupCategory:'',projects:[],writeups:[],catalog:[],search:''};
  const IS_GITHUB_PAGES=location.hostname.includes('github.io');
  const OWNER='hackus-mans';
  const REPO='portofiolo';
  const BRANCH='main';
  const LEGACY_BASE='/portofiolo';
  const SITE_BASE=(()=>{if(!IS_GITHUB_PAGES)return'';const first=(location.pathname.split('/').filter(Boolean)[0]||'').trim();return first&&!first.includes('.')?'/'+first:''})();

  function safe(v){return String(v||'')}
  function clean(v){return safe(v).trim()}
  function byId(id){return document.getElementById(id)}
  function esc(v){return clean(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}
  function assetPath(path){
    let p=clean(path);
    if(!p||/^(https?:|data:|blob:|mailto:|tel:|#)/i.test(p))return p;
    if(IS_GITHUB_PAGES){
      if(SITE_BASE&&p.startsWith(SITE_BASE+'/'))return p;
      if(SITE_BASE&&p.startsWith(LEGACY_BASE+'/')&&SITE_BASE!==LEGACY_BASE)return SITE_BASE+p.slice(LEGACY_BASE.length);
      if(p.startsWith(LEGACY_BASE+'/'))return p;
      if(p.startsWith('/'))return (SITE_BASE||'')+p;
      return p;
    }
    if(p.startsWith(LEGACY_BASE+'/'))return p.replace(LEGACY_BASE+'/','/');
    if(SITE_BASE&&p.startsWith(SITE_BASE+'/'))return p.slice(SITE_BASE.length)||'/';
    return p;
  }
  async function loadJSON(path){const r=await fetch(assetPath(path),{cache:'no-store'});if(!r.ok)throw new Error(path);return r.json()}
  async function loadText(path){const r=await fetch(assetPath(path),{cache:'no-store'});if(!r.ok)throw new Error(path);return r.text()}
  async function loadGithubFolder(folder){
    const api='https://api.github.com/repos/'+OWNER+'/'+REPO+'/contents/'+folder+'?ref='+BRANCH+'&cmsLive='+Date.now();
    const res=await fetch(api,{cache:'no-store'});
    if(!res.ok)return[];
    const files=await res.json();
    if(!Array.isArray(files))return[];
    const rows=[];
    for(const file of files.filter(f=>f.type==='file'&&String(f.name||'').toLowerCase().endsWith('.json'))){
      try{const r=await fetch(file.download_url+'?cmsLive='+Date.now(),{cache:'no-store'});if(r.ok)rows.push(await r.json())}catch(e){}
    }
    return rows;
  }
  function normalize(v){return clean(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
  function slugify(v){return normalize(v).replace(/\.[a-z0-9]+$/i,'').replace(/[^a-z0-9._-]+/g,'-').replace(/^-+|-+$/g,'')||'file'}
  function slugFile(name){const n=clean(name).split('|')[0].trim().replace(/^\/+/, '');if(n.includes('/'))return n.split('/').map(slugify).join('/');const ext=(n.match(/\.[A-Za-z0-9]+$/)||[''])[0];const base=ext?n.slice(0,-ext.length):n;return slugify(base)+ext.toLowerCase()}
  function asList(v){return Array.isArray(v)?v.map(clean).filter(Boolean):clean(v)?[clean(v)]:[]}
  function parseYaml(yaml){const obj={};let cur=null;safe(yaml).split('\n').forEach(line=>{const s=line.trim();if(!s)return;if(s.startsWith('- ')&&cur){obj[cur]=Array.isArray(obj[cur])?obj[cur]:[];obj[cur].push(s.slice(2).trim().replace(/^["']|["']$/g,''));return}const m=s.match(/^([A-Za-z0-9_-]+):\s*(.*)$/);if(m){cur=m[1];const v=m[2].trim().replace(/^["']|["']$/g,'');obj[cur]=v?v:[]}});return obj}
  function splitFM(md){const text=safe(md);if(!text.startsWith('---'))return{meta:{},body:text};const end=text.indexOf('\n---',3);if(end===-1)return{meta:{},body:text};return{meta:parseYaml(text.slice(3,end).trim()),body:text.slice(end+4).trim()}}
  function stripDecorations(text){return safe(text).replace(/<mark[^>]*>/gi,'').replace(/<\/mark>/gi,'').replace(/<font[^>]*>/gi,'').replace(/<\/font>/gi,'').replace(/!\[[^\]]*\]\([^)]+\)/g,'').replace(/!\[\[[^\]]+\]\]/g,'').trim()}
  function firstPara(body){const b=stripDecorations(body).replace(/^#\s+.*$/m,'').trim();const parts=b.split(/\n\s*\n/).map(p=>p.trim()).filter(p=>p&&!p.startsWith('#')&&!p.startsWith('```'));return parts[0]||'Documentation technique en cours de présentation.'}
  function uniqueBy(items,keyFn){const map=new Map();items.forEach(item=>{const k=keyFn(item);if(k&&!map.has(k))map.set(k,item)});return[...map.values()]}
  function projectPathCandidates(item){const out=[];const add=p=>{p=clean(p);if(!p)return;if(p.startsWith('/'))p=p.slice(1);if(!p.startsWith('content/projects/'))p='content/projects/'+p;if(!p.endsWith('.md'))p=p.replace(/\/$/,'')+'.md';if(!out.includes(p))out.push(p)};if(item.path)add(item.path);if(item.obsidianPath)add(item.obsidianPath);if(item.title)add(slugify(item.title)+'.md');if(item.publicTitle)add(slugify(item.publicTitle)+'.md');return out}
  function writeupPathCandidates(item){const out=[];const add=p=>{p=clean(p);if(!p)return;if(p.startsWith('/'))p=p.slice(1);if(!p.startsWith('content/writeups/'))p='content/writeups/'+p;if(!p.endsWith('.md'))p=p.replace(/\/$/,'')+'.md';if(!out.includes(p))out.push(p)};if(item.path)add(item.path);if(item.obsidianPath)add(item.obsidianPath);if(item.title)add(slugify(item.title)+'.md');if(item.publicTitle)add(slugify(item.publicTitle)+'.md');return out}
  async function loadFirstText(candidates){for(const path of candidates){try{return{path,content:await loadText(path)}}catch(e){}}return{path:candidates[0]||'',content:''}}
  async function loadProjectRows(){const rows=[];try{const control=await loadJSON('content/projects/project-control.json');if(Array.isArray(control.items))rows.push(...control.items)}catch(e){}try{rows.push(...await loadGithubFolder('content/projects-control'))}catch(e){}return uniqueBy(rows,item=>normalize(item.publicTitle||item.title||item.obsidianPath||item.path))}
  async function loadProjects(){try{const rows=(await loadProjectRows()).filter(i=>i.publish===true||String(i.publish).toLowerCase()==='true');return await Promise.all(rows.map(async item=>{const loaded=await loadFirstText(projectPathCandidates(item));const parsed=splitFM(loaded.content);const skills=asList(item.skills).length?asList(item.skills):asList(parsed.meta.skills);const stack=asList(item.tools).length?asList(item.tools):asList(parsed.meta.tools);return{...item,type:'project',title:clean(item.publicTitle||parsed.meta.title||item.title),category:clean(item.category||parsed.meta.category||'Projet'),level:clean(parsed.meta.level||'À définir'),status:clean(item.status||parsed.meta.status||'À documenter'),description:clean(item.description||firstPara(parsed.body)),stack,skills,github:clean(parsed.meta.github||'#'),demo:clean(parsed.meta.demo||'#'),documentation:parsed.body,path:loaded.path}}))}catch(e){return[]}}
  async function loadWriteupRows(){const rows=[];try{const d=await loadJSON('content/writeups/writeups.json');if(Array.isArray(d.items))rows.push(...d.items)}catch(e){}try{rows.push(...await loadGithubFolder('content/writeups-control'))}catch(e){}return uniqueBy(rows,item=>normalize(item.publicTitle||item.title||item.obsidianPath||item.path))}
  async function loadWriteups(){try{return await Promise.all((await loadWriteupRows()).filter(i=>i.publish!==false&&String(i.publish).toLowerCase()!=='false').map(async item=>{const loaded=await loadFirstText(writeupPathCandidates(item));const parsed=splitFM(loaded.content);return{...item,type:'writeup',title:clean(item.publicTitle||parsed.meta.title||item.title),description:clean(item.description||firstPara(parsed.body)),documentation:parsed.body,path:loaded.path}}))}catch(e){return[]}}
  async function loadCatalog(){try{const d=await loadJSON('content/writeups/writeups-catalog.json');return Array.isArray(d.items)?d.items:[]}catch(e){return[]}}
  function itemText(item){return normalize([item.title,item.description,item.category,item.platform,item.sectionLabel,item.status,item.difficulty,...asList(item.skills),...asList(item.tags),...asList(item.stack)].join(' '))}
  function matchSearch(item){const q=normalize(state.search);return !q||itemText(item).includes(q)}
  function mediaSrc(url){let u=clean(url).split('|')[0].trim();if(!u)return'';if(/^(https?:|data:|blob:)/i.test(u))return u;if(u.startsWith(LEGACY_BASE+'/'))return assetPath(u);if(u.startsWith('/content/')||u.startsWith('content/')||u.startsWith('/assets/')||u.startsWith('assets/'))return assetPath(u);return assetPath('content/writeups/media/'+slugFile(u))}
  function mediaHtml(alt,url){const src=mediaSrc(url);const c=clean(src).split('?')[0].split('#')[0].toLowerCase();const caption=esc(alt||clean(url).split('/').pop().split('|')[0]||'Pièce jointe');const s=esc(src);if(/\.(png|jpe?g|gif|webp|svg)$/i.test(c))return`<figure class="doc-media"><img src="${s}" alt="${caption}" loading="lazy"><figcaption>${caption}</figcaption></figure>`;if(/\.(mp3|wav|ogg|m4a|aac|flac)$/i.test(c))return`<figure class="doc-media"><audio controls src="${s}"></audio><figcaption>${caption}</figcaption></figure>`;if(/\.(mp4|webm|mov|mkv)$/i.test(c))return`<figure class="doc-media"><video controls src="${s}"></video><figcaption>${caption}</figcaption></figure>`;return`<p><a class="doc-attachment" href="${s}" target="_blank" rel="noopener">Ouvrir : ${caption}</a></p>`}
  function linkHref(url){return /^(https?:|mailto:|tel:|#)/i.test(clean(url))?clean(url):assetPath(url)}
  function inlineMarkdown(text){return text.replace(/&lt;mark[^&]*?&gt;/gi,'<span class="doc-highlight">').replace(/&lt;\/mark&gt;/gi,'</span>').replace(/&lt;font[^&]*?&gt;/gi,'<span class="doc-highlight">').replace(/&lt;\/font&gt;/gi,'</span>').replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>')}
  function mdToHtml(md){let text=safe(md);const codes=[];text=text.replace(/```([a-zA-Z0-9_-]*)\n([\s\S]*?)```/g,(_,lang,code)=>{const token=`@@CODE${codes.length}@@`;codes.push(`<pre><code>${esc(code)}</code></pre>`);return token});text=esc(text);text=text.replace(/^---+$/gm,'<hr>').replace(/^### (.*)$/gm,'<h4>$1</h4>').replace(/^## (.*)$/gm,'<h3>$1</h3>').replace(/^# (.*)$/gm,'<h2>$1</h2>');text=text.replace(/!\[\[([^\]]+)\]\]/g,(_,ref)=>mediaHtml(ref,ref)).replace(/!\[(.*?)\]\((.*?)\)/g,(_,alt,url)=>mediaHtml(alt,url)).replace(/\[(.*?)\]\((.*?)\)/g,(_,label,url)=>`<a href="${esc(linkHref(url))}" target="_blank" rel="noopener">${label}</a>`).replace(/^[*-] (.*)$/gm,'<li>$1</li>').replace(/(<li>.*<\/li>\n?)+/g,m=>`<ul>${m}</ul>`);text=text.split(/\n\s*\n/).map(b=>b.trim()).filter(Boolean).map(b=>/^<(h\d|ul|pre|figure|hr|p><a|img|video|audio)/.test(b)?b:`<p>${inlineMarkdown(b).replace(/\n/g,'<br>')}</p>`).join('\n');codes.forEach((h,i)=>text=text.replace(`@@CODE${i}@@`,h));return text}
  function closeReader(){const m=byId('realisation-reader');if(m)m.remove();document.body.classList.remove('modal-open')}
  async function openItem(item){let body=item.documentation||'';try{if(item.path){body=splitFM(await loadText(item.path)).body}}catch(e){body=`# ${clean(item.title)}\n\nImpossible de charger le fichier complet pour le moment.\n\n${clean(item.description)}`}
    closeReader();const skills=asList(item.skills).map(t=>`<span class="tag">${esc(t)}</span>`).join('');const tags=asList(item.tags||item.stack).map(t=>`<span class="tag">${esc(t)}</span>`).join('');const modal=document.createElement('div');modal.className='realisation-reader';modal.id='realisation-reader';modal.innerHTML=`<div class="real-reader-backdrop" data-close-reader="true"></div><section class="real-reader-panel pro-reader" role="dialog" aria-modal="true"><header class="real-reader-header pro-reader-head"><div><span class="card-kicker">${esc(item.sectionLabel||item.type)} • ${esc(item.platform||item.category)}</span><h2>${esc(item.title)}</h2><p>${esc(item.description)}</p></div><button class="real-reader-close" type="button" data-close-reader="true">Fermer</button></header><div class="real-reader-layout"><aside class="real-reader-sidebar"><p class="sidebar-title">Informations</p><div class="real-meta"><span>${esc(item.sectionLabel||'Projet')}</span><span>${esc(item.platform||item.status||'Publié')}</span><span>${esc(item.category||item.difficulty||'À définir')}</span></div>${skills?`<h4>Compétences acquises</h4><div class="tags">${skills}</div>`:''}${tags?`<h4>Tags / outils</h4><div class="tags">${tags}</div>`:''}</aside><article class="real-reader-document markdown-body obsidian-doc pro-doc">${mdToHtml(body)}</article></div></section>`;document.body.appendChild(modal);document.body.classList.add('modal-open');modal.querySelectorAll('[data-close-reader="true"]').forEach(b=>b.addEventListener('click',closeReader));setTimeout(()=>document.dispatchEvent(new Event('DOMContentLoaded')),10)}
  function card(item,i){const skills=asList(item.skills).map(t=>`<span class="tag">${esc(t)}</span>`).join('');const tags=asList(item.stack||item.tags).map(t=>`<span class="tag">${esc(t)}</span>`).join('');const kicker=item.type==='writeup'?`${esc(item.sectionLabel)} • ${esc(item.platform)} • ${esc(item.category)}`:`Projet • ${esc(item.category)}`;const status=item.type==='writeup'?clean(item.difficulty||item.status||'Publié'):clean(item.status||'À documenter');const label=item.type==='writeup'?'Lire le writeup':'Lire le projet';return`<article class="real-card" data-index="${i}"><div class="real-card-head"><span class="card-kicker">${kicker}</span><div class="real-meta"><span>${esc(status)}</span></div></div><h3>${esc(item.title)}</h3><p>${esc(item.description)}</p>${skills?`<div class="tags">${skills}</div>`:''}${tags?`<div class="tags">${tags}</div>`:''}<div class="real-actions"><button class="btn primary real-open" type="button">${label}</button></div></article>`}
  function branch(label,active,level){return`<button class="branch-card ${active?'active':''}" type="button" data-level="${level}" data-value="${esc(label)}"><span>${esc(label)}</span></button>`}
  function aggregate(list,key,filterFn=()=>true){const set=new Set();list.filter(filterFn).forEach(x=>{if(x[key])set.add(x[key])});return[...set]}
  function refreshControls(){const controls=byId('writeup-controls');if(!controls)return;controls.hidden=state.mode!=='writeup'||!!state.search;if(state.mode!=='writeup'||state.search)return;const cat=state.catalog.length?state.catalog:state.writeups.map(w=>({sectionLabel:w.sectionLabel,platform:w.platform,category:w.category}));const sections=['Challenges','Machine/Lab'].filter(s=>cat.some(x=>x.sectionLabel===s));const platforms=state.writeupSection?aggregate(cat,'platform',x=>x.sectionLabel===state.writeupSection):[];const categories=state.writeupPlatform?aggregate(cat,'category',x=>x.sectionLabel===state.writeupSection&&x.platform===state.writeupPlatform):[];controls.innerHTML=`<div class="writeup-browser-head"><div><p class="eyebrow">Navigation Writeups</p><h2>Choisis progressivement</h2></div></div><div class="writeup-step-stack"><section class="browser-level browser-level-wide"><div class="level-title"><span>01</span><h3>Type</h3></div><div class="branch-list branch-list-large">${sections.map(s=>branch(s,s===state.writeupSection,'section')).join('')||'<p class="level-empty">Aucune section détectée.</p>'}</div></section>${state.writeupSection?`<section class="browser-level browser-level-wide"><div class="level-title"><span>02</span><h3>Plateforme</h3></div><div class="branch-list branch-list-large">${platforms.map(p=>branch(p,p===state.writeupPlatform,'platform')).join('')}</div></section>`:''}${state.writeupPlatform?`<section class="browser-level browser-level-wide"><div class="level-title"><span>03</span><h3>Catégorie</h3></div><div class="branch-list branch-list-large">${categories.map(c=>branch(c,c===state.writeupCategory,'category')).join('')}</div></section>`:''}</div>`;controls.querySelectorAll('.branch-card').forEach(btn=>btn.addEventListener('click',()=>{const l=btn.dataset.level,v=btn.dataset.value;if(l==='section'){state.writeupSection=v;state.writeupPlatform='';state.writeupCategory=''}if(l==='platform'){state.writeupPlatform=v;state.writeupCategory=''}if(l==='category')state.writeupCategory=v;renderList()}))}
  function currentItems(){if(state.mode==='project')return state.projects.filter(matchSearch);if(state.search)return state.writeups.filter(matchSearch);if(!state.writeupSection||!state.writeupPlatform||!state.writeupCategory)return[];return state.writeups.filter(w=>w.sectionLabel===state.writeupSection&&w.platform===state.writeupPlatform&&w.category===state.writeupCategory&&matchSearch(w))}
  function stepMessage(){if(state.search)return'Aucun résultat trouvé pour cette recherche.';if(!state.writeupSection)return'Choisis d’abord Challenges ou Machine/Lab, ou utilise la barre de recherche.';if(!state.writeupPlatform)return'Choisis maintenant une plateforme.';if(!state.writeupCategory)return'Choisis maintenant une catégorie.';return'Aucun contenu disponible pour cette sélection.'}
  function renderList(){const box=byId('realisations-list');if(!box)return;refreshControls();const data=currentItems();if(!data.length){box.innerHTML=`<div class="real-empty">${state.mode==='writeup'?stepMessage():'Aucun projet publié ou aucun résultat trouvé.'}</div>`;return}box.innerHTML=(state.search?`<div class="real-search-result">${data.length} résultat(s) trouvé(s)</div>`:'')+data.map((item,i)=>card(item,i)).join('');box.querySelectorAll('.real-open').forEach(btn=>btn.addEventListener('click',()=>openItem(data[Number(btn.closest('.real-card').dataset.index)])))}
  function initTabs(){document.querySelectorAll('.real-tab').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.real-tab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');state.mode=btn.dataset.filter==='writeup'?'writeup':'project';state.search='';const input=byId('realisation-search');if(input)input.value='';if(state.mode==='writeup'){state.writeupSection='';state.writeupPlatform='';state.writeupCategory=''}renderList()}))}
  function initSearch(){const input=byId('realisation-search');const clear=byId('realisation-search-clear');if(!input)return;input.addEventListener('input',()=>{state.search=input.value.trim();renderList()});if(clear)clear.addEventListener('click',()=>{input.value='';state.search='';renderList();input.focus()})}
  async function init(){const box=byId('realisations-list');if(!box)return;const[projects,writeups,catalog]=await Promise.all([loadProjects(),loadWriteups(),loadCatalog()]);state.projects=projects;state.writeups=writeups;state.catalog=catalog;initTabs();initSearch();renderList()}
  setTimeout(init,350);
})();
